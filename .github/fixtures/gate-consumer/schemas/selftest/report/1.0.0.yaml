# GENERATED BY `contract init` — PLACEHOLDER. Replace REPLACE_ME_* below with the real fields of
# your deliverable before promoting this boundary past `mode: observe` (see contract.yaml).
schema: selftest.report
version: 1.0.0
kind: payload
fields:
  - name: REPLACE_ME_FIELD
    type: string
    required: true
