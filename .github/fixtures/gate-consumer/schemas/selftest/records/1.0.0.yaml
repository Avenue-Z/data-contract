# GENERATED BY `contract init` — PLACEHOLDER. Replace REPLACE_ME_* below with the real columns of
# the normalized rows before promoting this boundary past `mode: observe` (see contract.yaml).
schema: selftest.records
version: 1.0.0
kind: tabular
fields:
  - name: REPLACE_ME_COLUMN
    type: string
    required: true
