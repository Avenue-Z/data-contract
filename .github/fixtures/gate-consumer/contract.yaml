# GENERATED BY `contract init` — safe to edit; `init` never overwrites this file.
system: gate-selftest
version: 0.1.0
raw:
  - name: selftest_raw
    schema: selftest.raw_report@1
    source: {kind: api, format: json}
    mode: observe
inputs:
  # No `source:` — the normalized input is produced by an adapter over the raw payload
  # (`normalize(raw)` in boundaries.py), not read independently from the vendor.
  - name: records
    schema: selftest.records@1
    mode: observe
outputs:
  - name: report
    schema: selftest.report@1
    sink: {kind: file, format: json}
    mode: observe
